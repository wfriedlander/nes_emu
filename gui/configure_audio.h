#ifndef CONFIGURE_AUDIO_H
#define CONFIGURE_AUDIO_H

#include <QDialog>

class AudioWindow : public QDialog
{
    Q_OBJECT
public:
    explicit AudioWindow(QWidget *parent = nullptr);

signals:

};

#endif // CONFIGURE_AUDIO_H
